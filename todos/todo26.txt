Analyzing all 6 issues. Exploring the codebase to understand the current implementation.


[7 tools called]


Reviewing key files to understand the issues:


[6 tools called]


Searching for best practices and checking how data is passed to views:


[6 tools called]


## Detailed analysis of 6 issues

### Issue 1: Dashboard dropdown image not showing; other pages showing only first character

Root cause:
- Dashboard: `user.profileImage` is passed correctly in `dashboardPageController.ts` (line 116), but the header may not receive it consistently.
- Discover/Likes pages: `discoverPageController.ts` uses `res.locals.user`, which may not include `profileImage`.

Code issues:
```118:124:frontend/src/views/header.ejs
<% if (user.profileImage) { %>
    <img src="<%= user.profileImage %>" alt="Profile" class="w-10 h-10 rounded-full object-cover shadow-lg ring-2 ring-white dark:ring-gray-700">
<% } else { %>
    <div class="w-10 h-10 bg-gradient-to-r from-primary via-accent to-purple-500 rounded-full flex items-center justify-center text-white font-bold shadow-lg ring-2 ring-white dark:ring-gray-700">
        <%= (user.handle || user.email || 'U').charAt(0).toUpperCase() %>
    </div>
<% } %>
```

Solution:
1. Ensure middleware populates `res.locals.user` with `profileImage`.
2. Verify image paths are absolute or correctly relative.
3. Add error handling for broken image URLs.

---

### Issue 2: Disabled likes/share/follow should show button state + hover message, not 403

Current behavior:
- Follow button: shows disabled state (good).
- Like/Share buttons: return 403 on click (bad UX).

Best practice (from research):
- Disable the button visually and functionally.
- Show a tooltip on hover explaining why it's disabled.
- Prevent the click event from firing.

Current code issue:
```662:673:frontend/src/views/discover.ejs
async function toggleLike(twinId, button) {
    try {
        const response = await fetch('/api/social/toggle-like', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify({ twinId })
        });

        const data = await response.json();
```

Solution:
1. Check `allowLikes`, `allowFollows`, `allowShares` when rendering cards.
2. Disable buttons and add tooltips if disabled.
3. Prevent click handlers from firing for disabled buttons.
4. Backend should still return 403, but frontend should block the request.

---

### Issue 3: Settings page reload redirects to account tab

Root cause:
- On reload, `profile.ejs` defaults to the 'account' tab (line 749):
```748:749:frontend/src/views/profile.ejs
// Settings tabs - default to first tab
switchSettingsTab('account');
```

Solution:
1. Read the active tab from the URL query parameter (`?tab=settings&subtab=privacy`).
2. Use `window.history.pushState` to update the URL without reload.
3. On page load, restore the tab from the URL.

---

### Issue 4: Admin analytics — multiple issues (events not showing, can't click, stats not working)

Issues found:
1. Charts may not render if Chart.js isn't loaded or canvas elements aren't ready.
2. Event data may not be formatted correctly.
3. Click handlers may not be attached properly.
4. Time-based analytics may have API issues.

Code issues:
```602:629:frontend/src/views/admin-analytics.ejs
function createEventBreakdownChart(eventBreakdown) {
    const ctx = document.getElementById('eventBreakdownChart').getContext('2d');
    new Chart(ctx, {
```

Recommendation:
- Keep the current system and fix it:
  - Add error handling for chart rendering.
  - Verify API responses.
  - Add loading states.
  - Fix click event handlers.
- Consider Google Analytics later for additional insights, but not as a replacement.

---

### Issue 5: Charts/graphs not showing in /analytics and other places

Root causes:
1. Chart.js not loaded or loaded after chart initialization.
2. Canvas elements not in the DOM when charts are created.
3. Data format mismatch.
4. JavaScript errors preventing execution.

Code issues:
```94:105:frontend/src/views/partials/analytics-body.ejs
<!-- Chart Container -->
<div id="chartContainer" class="h-64 flex items-center justify-center text-gray-500 dark:text-gray-400">
    <p>Chart will be loaded here</p>
</div>
```

Solution:
1. Ensure Chart.js loads before initialization.
2. Initialize charts after DOM is ready.
3. Add error handling and fallbacks.
4. Verify data format matches Chart.js expectations.

---

### Issue 6: Backend data leaks — user IDs and sensitive info exposed

Security issues found:
1. User IDs exposed in frontend:
```1231:1231:frontend/src/views/admin-analytics.ejs
<div class="flex items-center justify-between p-4 bg-gray-50 rounded-lg hover:bg-gray-100 transition-colors cursor-pointer" onclick="viewUserDetails('${user.id}')">
```

2. Twin IDs exposed:
```679:679:frontend/src/views/dashboard.ejs
<% if (twin) { %>data-twin-id="<%= twin.id %>" data-list-type="chatters"<% } %>>
```

3. Chat IDs exposed:
```1412:1412:frontend/src/views/chat-enhanced.ejs
<div class="chat-item p-3 border-b dark:border-gray-700 cursor-pointer hover:bg-gray-50 dark:hover:bg-gray-700/50 rounded-lg relative group ${chat.id === currentChatId ? 'bg-blue-50 dark:bg-blue-900/30 border-blue-200 dark:border-blue-800' : ''}" data-chat-id="${chat.id}">
```

Best practices (from research):
1. Use opaque identifiers (hashed/salted) for public-facing IDs.
2. Sanitize all user data before sending to frontend.
3. Implement proper authorization checks.
4. Mask sensitive data in logs and responses.

Solution:
1. Create a mapping service that converts internal IDs to public tokens.
2. Sanitize all responses to remove unnecessary sensitive fields.
3. Add authorization middleware to verify user access.
4. Review all API endpoints for data exposure.

---

## Priority action plan

Immediate (high priority):
1. Fix image display (Issue 1) — ensure `profileImage` is passed consistently.
2. Fix disabled button UX (Issue 2) — add proper disabled states and tooltips.
3. Fix settings reload (Issue 3) — preserve tab state in URL.

Medium priority:
4. Fix admin analytics (Issue 4) — add error handling and verify API responses.
5. Fix charts not rendering (Issue 5) — ensure Chart.js loads and initialize after DOM ready.

High priority (security):
6. Fix data leaks (Issue 6) — implement ID masking and sanitization.

Should I start with Issues 1–3, or focus on a specific one?
Database mein unused features check kar raha hoon:


[7 tools called]





[4 tools called]


Database mein unused features check kar raha hoon:


[4 tools called]


Final plan with unused features:

## Final plan — settings & unused features

### Unused/missing features in database

#### 1. ContentReport table — partially used
- Status: API exists (`/api/moderation/report`), UI missing
- Action: Add "Report Twin" button on public profile page
- Settings: Not needed (public feature)

#### 2. ReferralCode — used but UI missing
- Status: Backend working, settings UI missing
- Action: Add "Referral Program" section in Account & Security tab
- Show: My Referral Code, Referral Link, Total Referrals

#### 3. Verified badge — exists but no admin control
- Status: Field exists, no admin UI to verify twins
- Action: Admin-only feature (not in user settings)

#### 4. Chat.title, Chat.summary — used
- Status: Being used in chatSharedUtils
- Action: No changes needed

#### 5. PublicChat.title, PublicChat.summary — partially used
- Status: Fields exist, may not be shown in UI
- Action: Verify if shown in public-chat-history page

#### 6. ChatFeedback table — old schema
- Status: Table exists but uses INTEGER for userId (should be TEXT)
- Action: Migration needed if using this table

#### 7. AILearning table — old schema
- Status: Table exists but uses INTEGER (should be TEXT)
- Action: Migration needed if using this table

---

## Final settings structure

### Tab 1: Account & Security
- Change Password
- Referral Program (NEW)
  - My Referral Code
  - Referral Link
  - Total Referrals Count
- Export My Data
- Delete Account

### Tab 2: Twin Visibility
- Make Twin Public/Private (toggle)
- Public Handle (editable)
- Bio (editable)
- Profile Image (upload)

### Tab 3: Privacy & Interactions

Chat Settings:
- Require Login (toggle) — "Force users to log in before chatting"
- Show Chat History (toggle) — "Make previous chats visible to others"
- Require Approval (toggle) — "Review messages before they're sent"

Social Interactions:
- Allow Likes (toggle)
- Allow Follows (toggle)
- Allow Shares (toggle)

Blocked Users:
- List of blocked users
- + Block User button

### Tab 4: Appearance
- Theme Toggle (Dark/Light)

---

## Implementation plan

### Phase 1: Remove redundant fields (database cleanup)

Remove from Twin table:
1. `allowPublicChat` — use `isPublic` check
2. `allowAnonymousChat` — use `requireLogin` (inverted)
3. `moderateMessages` — use `requireApproval`
4. `allowDirectMessages` — feature doesn't exist

Migration:
```sql
-- Remove redundant columns (keep data for 30 days, then drop)
ALTER TABLE "Twin" DROP COLUMN IF EXISTS "allowPublicChat";
ALTER TABLE "Twin" DROP COLUMN IF EXISTS "allowAnonymousChat";
ALTER TABLE "Twin" DROP COLUMN IF EXISTS "moderateMessages";
ALTER TABLE "Twin" DROP COLUMN IF EXISTS "allowDirectMessages";
```

### Phase 2: Add missing validations

A. Blocked users check:
```typescript
// In startPublicChat & sendPublicMessage:
if (userId) {
  const isBlocked = await db.query(`
    SELECT id FROM "TwinBlockedUsers"
    WHERE "twinId" = $1 AND "userId" = $2
  `, [twinId, userId]);
  
  if (isBlocked.rows.length > 0) {
    throw createError.forbidden('You are blocked from chatting with this twin');
  }
}
```

B. Require login check:
```typescript
// In startPublicChat:
if (twin.requireLogin && !req.user) {
  throw createError.unauthorized('Login required to chat with this twin');
}
```

C. Allow Likes/Follows/Shares check:
```typescript
// In likeTwin API:
if (!twin.allowLikes) {
  throw createError.forbidden('Likes are disabled for this twin');
}
// Similar for followTwin and shareTwin
```

D. Show chat history:
```typescript
// In getPublicChatHistory:
if (!twin.showChatHistory && chat.userId !== req.user?.id) {
  // Only show current chat, not previous chats
  return onlyCurrentChat;
}
```

### Phase 3: Add new features

A. Referral program in settings:
- Show referral code
- Copy referral link
- Show referral count
- API: `/api/invite/my-code` (already exists)

B. Report content button:
- Add "Report" button on public profile page
- API: `/api/moderation/report` (already exists)

### Phase 4: Update frontend

1. Remove redundant toggles:
   - Remove `allowPublicChat`
   - Remove `allowAnonymousChat`
   - Remove `moderateMessages`
   - Remove `allowDirectMessages`

2. Update `requireLogin` logic:
   - Inverted: `true` = login required, `false` = anonymous allowed

3. Add Referral Program section:
   - In Account & Security tab
   - Show code, link, count

4. Add Report button:
   - On public profile page
   - Modal with reason selection

---

## Summary — what to keep/remove/add

### Remove (redundant)
1. `allowPublicChat` — use `isPublic`
2. `allowAnonymousChat` — use `requireLogin`
3. `moderateMessages` — use `requireApproval`
4. `allowDirectMessages` — feature doesn't exist

### Keep & implement
1. `requireLogin` — with validation
2. `showChatHistory` — with implementation
3. `allowLikes/Follows/Shares` — with API validation
4. `requireApproval` — already exists, use this
5. Blocked Users — with chat validation

### Add (new features)
1. Referral Program — in Account & Security tab
2. Report Content — button on public profile page

### Priority order
1. High: Blocked users check in chat
2. High: `requireLogin` validation
3. High: `allowLikes/Follows/Shares` validation
4. Medium: `showChatHistory` implementation
5. Medium: Add Referral Program to settings
6. Medium: Add Report button to public profile
7. Low: Remove redundant fields (database cleanup)

---

## Final settings UI structure

### Tab 1: Account & Security
```
- Change Password
- Referral Program (NEW)
  ├─ My Referral Code: ABC123
  ├─ Referral Link: [Copy Button]
  └─ Total Referrals: 5
- Export My Data
- Delete Account
```

### Tab 2: Twin Visibility
```
- Make Twin Public/Private [Toggle]
- Public Handle: [Input]
- Bio: [Textarea]
- Profile Image: [Upload]
```

### Tab 3: Privacy & Interactions
```
Chat Settings:
├─ Require Login [Toggle]
├─ Show Chat History [Toggle]
└─ Require Approval [Toggle]

Social Interactions:
├─ Allow Likes [Toggle]
├─ Allow Follows [Toggle]
└─ Allow Shares [Toggle]

Blocked Users:
├─ [List of blocked users]
└─ [+ Block User Button]
```

### Tab 4: Appearance
```
- Theme: [Dark/Light Toggle]
```

---

Yeh final plan implement karein?
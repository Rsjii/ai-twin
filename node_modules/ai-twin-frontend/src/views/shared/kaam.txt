<!-- 
    Reusable Pagination Component
    
    Usage:
    <%- include('shared/pagination', {
        containerId: 'paginationContainer',  // Required: ID of container div
        loadFunction: 'loadPublicChatHistory', // Required: Function name to call on page change
        scrollToTop: true,                     // Optional: Scroll to top on page change (default: true)
        itemsPerPage: 5,                      // Optional: Items per page (default: 10)
        className: 'mt-8'                     // Optional: Additional CSS classes
    }) %>
-->

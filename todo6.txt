Checking the codebase for common functions, dead code, unoptimized queries, and duplicated/redundant code. Exploring the structure first.


[3 tools called]


Checking for duplicated functions, dead code, and unoptimized queries:


[4 tools called]


Checking for duplicate functions and unused code:


[6 tools called]


Checking for unused routes, dead code, and unoptimized queries:


[5 tools called]


Checking for more specific issues:


[6 tools called]


Checking a few more areas:


[3 tools called]


## Codebase analysis report

### 1. Duplicate functions

#### A. `getChatHistory` — 4 implementations
- `backend/src/modules/chat/chatController.ts` (line 193) — API endpoint
- `backend/src/modules/chat/chatManagementController.ts` (line 26) — Different query structure
- `backend/src/modules/chat/enhancedChatController.ts` (line 207) — Enhanced version
- `backend/src/controllers/chatPageController.ts` (line 118) — Page renderer (different purpose)

Issue: Three API implementations with different logic. Consolidate into one.

#### B. `sanitizeText` — duplicate implementations
- `backend/src/middleware/security.ts` (line 39) — Basic sanitization
- `backend/src/utils/safety.ts` (line 121) — Advanced sanitization with pattern removal

Issue: Two different implementations. Choose one and standardize.

#### C. `validateMessageLength` — duplicate implementations
- `backend/src/middleware/security.ts` (line 52) — `maxLength` only
- `backend/src/utils/safety.ts` (line 144) — `minLength` and `maxLength`

Issue: Different signatures. Standardize on one.

#### D. `fastQuery` helper — duplicated
- `backend/src/controllers/twinManagePageController.ts` (line 28)
- `backend/src/modules/twin/twinPerformanceController.ts` (line 18)

Issue: Duplicate helper. Extract to a shared utility.

#### E. Blacklist checking — duplicate logic
- `backend/src/middleware/security.ts` — `checkBlacklist()`
- `backend/src/utils/safety.ts` — `hasBannedWords()`, `hasImpersonationRisk()`, `isContentSafe()`

Issue: Overlapping functionality. Consolidate.

---

### 2. Dead code

#### A. Deprecated functions
1. `updateChatMetadata` in `backend/src/modules/chat/chatController.ts` (line 1064)
   - Marked deprecated, disabled, but still exported
   - Action: Remove or fully remove if unused

2. `getMemoryStats` in `backend/src/modules/memory/memoryController.ts` (line 10)
   - Marked deprecated but still active
   - Action: Remove or mark for removal

#### B. Test routes (production risk)
- `backend/src/controllers/testController.ts` — All test endpoints
  - `/test`, `/test-session`, `/test-db`, `/test-auth`, `/test-otp`, `/basic`, `/test-profile`
  - Action: Disable in production or remove

#### C. Debug code
- Multiple `console.log` statements (109 instances found)
  - Files: `chatController.ts`, `twinController.ts`, `inviteController.ts`, `adminAnalyticsController.ts`, `jwtCookie.ts`, etc.
  - Action: Replace with logger or remove

#### D. Commented code
- `backend/src/app.ts` (lines 1-2) — Random comments
- `backend/src/modules/chat/chatController.ts` (lines 1070-1136) — Large commented block in `updateChatMetadata`

---

### 3. Unoptimized queries/functions

#### A. N+1 query patterns
1. `backend/src/modules/chat/chatController.ts` (line 621-632)
   ```typescript
   // Debug query left in production code
   const allChatsResult = await db.query(`
     SELECT c.id, c."userId", c."twinId"
     FROM "Chat" c
     WHERE c."userId" = $1
     LIMIT 5
   `, [req.user.id]);
   ```
   - Action: Remove debug queries

2. Multiple queries in loops potential:
   - `backend/src/controllers/twinManagePageController.ts` — Multiple sequential `fastQuery` calls
   - Could be optimized with `Promise.all()`

#### B. Missing query optimization
1. `backend/src/modules/chat/chatController.ts` (line 199-220)
   - Uses `LEFT JOIN LATERAL` — could be optimized
   - No LIMIT on message count subquery

2. `backend/src/modules/chat/chatManagementController.ts` (line 35-51)
   - No pagination (LIMIT 50 hardcoded)
   - Could benefit from cursor-based pagination

#### C. Inefficient database operations
1. `backend/src/config/db.ts` (line 33-56)
   - Retry logic with 3 attempts and delays
   - May cause slow failures
   - Action: Review retry strategy

2. `backend/src/modules/chat/publicChatController.ts` (line 611)
   - Async performance update that may fail silently
   - Action: Add proper error handling

---

### 4. Unused/redundant code

#### A. Unused imports (potential)
- Check with TypeScript compiler (`noUnusedLocals: true` in tsconfig)
- Many files may have unused imports

#### B. Redundant validation
- Multiple validation layers doing similar checks:
  - Zod schemas in `validation.ts`
  - Manual checks in controllers
  - Middleware validation
  - Action: Consolidate validation logic

#### C. Duplicate error handling
- Similar try-catch patterns repeated across controllers
- Action: Create wrapper or middleware

#### D. Unused route handlers
- Some routes may be defined but not used
- Action: Audit route usage

---

### 5. Code quality issues

#### A. Inconsistent error handling
- Mix of `createError.*` and direct `AppError` throws
- Some functions use `next(error)`, others throw
- Action: Standardize error handling

#### B. Type safety issues
- `req: any` used in many places
- Missing proper typing for authenticated requests
- Action: Use proper types

#### C. Hardcoded values
- Magic numbers (e.g., `LIMIT 50`, `maxLength: 300`)
- Action: Move to config constants

---

### 6. Security concerns

#### A. Test endpoints exposed
- Test routes accessible in production
- Action: Add environment check or remove

#### B. Console logging sensitive data
- Some `console.log` may log user data
- Action: Remove or use logger with proper levels

---

### Summary statistics

| Category | Count | Priority |
|----------|-------|----------|
| Duplicate Functions | 5+ | High |
| Dead Code | 3 major | Medium |
| Unoptimized Queries | 5+ | High |
| Console.log statements | 109 | Low |
| Test Routes | 7 | Medium |
| Type Safety Issues | Many | Medium |

---

### Recommended actions (priority order)

1. High priority
   - Consolidate `getChatHistory` implementations
   - Merge `sanitizeText` and `validateMessageLength` implementations
   - Remove or disable test routes in production
   - Extract `fastQuery` to shared utility
   - Remove debug queries

2. Medium priority
   - Remove deprecated functions (`updateChatMetadata`, `getMemoryStats`)
   - Replace `console.log` with logger
   - Optimize N+1 query patterns
   - Consolidate validation logic

3. Low priority
   - Clean up commented code
   - Standardize error handling
   - Improve type safety
   - Extract hardcoded values to config

Should I start with the high-priority items?